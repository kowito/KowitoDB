[package]
name = "embedvec-bench"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
embedvec = "0.8"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
serde_json = "1"
